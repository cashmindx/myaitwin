/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}',
  ],
  theme: {
    extend: {
      // Optional: Customize colors, fonts, spacing here
    },
  },
  plugins: [
    // Optional: Add plugins like require('@tailwindcss/forms')
  ],
};
